{
  "name": "@pms/shared-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared API contracts between backend and frontend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate": "ts-node scripts/generate-types.ts",
    "build": "tsc",
    "clean": "rm -rf dist types"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "openapi-typescript": "^6.7.6",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "zod": "^4.4.3"
  }
}
